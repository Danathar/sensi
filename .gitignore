__pycache__
*.py.b*
secrets.yaml

# Logs
*.log
pip-log.txt

# Unit test / coverage reports
.coverage
coverage.xml
nosetests.xml
htmlcov/
test-reports/
test-results.xml
test-output.xml
pytest-*.txt

# venv stuff
pyvenv.cfg
pip-selfcheck.json
venv
.venv
Pipfile*
share/*

# Visual Studio Code
.vscode/*
.env

# Key and secret shapes the Bash gate treats as withheld, so `git add .`
# cannot stage one dropped beside the checkout (git diff --cached would print it)
.env.*
*.pem
*.p12
cosign.key
id_rsa
id_ed25519

# mypy
/.mypy_cache/*
/.dmypy.json

# These come from the image
pyproject.toml
.pre-commit-config.yaml
pylint/
config/
.vscode/
